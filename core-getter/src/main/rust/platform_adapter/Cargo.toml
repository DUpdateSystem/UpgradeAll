[package]
name = "upgradeall-platform-adapter"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "1"

[target.'cfg(target_os = "android")'.dependencies]
jni = "0.21"
once_cell = "1"

[dev-dependencies]
getter-core = { path = "../getter/crates/getter-core" }
