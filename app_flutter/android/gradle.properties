org.gradle.jvmargs=-Xmx4G
android.useAndroidX=true
android.enableJetifier=true
# Flutter's Gradle plugin still expects AGP's legacy Android extension while
# Flutter 3.44 templates use AGP 9, so keep the template compatibility flags
# until Flutter removes them upstream.
android.newDsl=false
android.builtInKotlin=false
android.suppressUnsupportedCompileSdk=36
